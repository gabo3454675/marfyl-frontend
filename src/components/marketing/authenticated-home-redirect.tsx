'use client';

/**
 * @deprecated Usar `MarketingPreviewBar`. Se mantiene el nombre del módulo por compatibilidad con cachés de build.
 */
export { MarketingPreviewBar as AuthenticatedHomeRedirect } from './marketing-preview-bar';
